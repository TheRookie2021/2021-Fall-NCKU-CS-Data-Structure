#include<stdio.h>
#include<stdlib.h>

typedef struct {
    int edge;
    struct Node * next; 
}Node;

typedef struct{
    struct Node *head; 
}List;

List CreatList(){
    List list;
    Node* newnode=malloc(sizeof(Node));
    

}


int main(){
   

    return 0;
}